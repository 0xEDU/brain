{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "clean": "nuxt cleanup",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "predeploy": "nuxt generate",
    "deploy": "nuxt generate && netlify deploy --prod --dir=dist",
    "postdeploy": "nuxt cleanup"
  },
  "dependencies": {
    "@mdi/font": "^7.4.47",
    "nuxt": "^3.10.3",
    "vue": "^3.4.19",
    "vue-router": "^4.3.0"
  },
  "devDependencies": {
    "vite-plugin-vuetify": "^2.0.2",
    "vuetify": "^3.5.6"
  }
}
